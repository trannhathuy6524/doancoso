@page
@model GotoCarRental.Areas.Admin.Pages.Features.CreateModel
@{
    ViewData["Title"] = "Thêm tính năng mới";
}

<div class="container py-4">
    <div class="row">
        <div class="col-md-8">
            <h1 class="display-6 fw-bold">Thêm tính năng mới</h1>
            <nav aria-label="breadcrumb">
                <ol class="breadcrumb">
                    <li class="breadcrumb-item"><a asp-area="Admin" asp-page="/Index">Trang chủ</a></li>
                    <li class="breadcrumb-item"><a asp-page="./Index">Tính năng xe</a></li>
                    <li class="breadcrumb-item active" aria-current="page">Thêm mới</li>
                </ol>
            </nav>
        </div>
    </div>

    <div class="row mt-3">
        <div class="col-md-8">
            <div class="card shadow-sm">
                <div class="card-body">
                    <form method="post">
                        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                        
                        <div class="mb-3">
                            <label asp-for="Feature.Name" class="form-label">Tên tính năng</label>
                            <input asp-for="Feature.Name" class="form-control" />
                            <span asp-validation-for="Feature.Name" class="text-danger"></span>
                        </div>
                        
                        <div class="mb-3">
                            <label asp-for="Feature.Description" class="form-label">Mô tả</label>
                            <textarea asp-for="Feature.Description" class="form-control" rows="3"></textarea>
                            <span asp-validation-for="Feature.Description" class="text-danger"></span>
                            <div class="form-text">Mô tả chi tiết về tính năng này (không bắt buộc).</div>
                        </div>
                        
                        <div class="d-flex justify-content-end mt-4">
                            <a asp-page="./Index" class="btn btn-outline-secondary me-2">
                                <i class="bi bi-arrow-left me-1"></i> Quay lại
                            </a>
                            <button type="submit" class="btn btn-primary">
                                <i class="bi bi-save me-1"></i> Lưu
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
        
        <div class="col-md-4">
            <div class="card shadow-sm">
                <div class="card-header bg-light py-3">
                    <h5 class="mb-0">Hướng dẫn</h5>
                </div>
                <div class="card-body">
                    <p>Tính năng xe sẽ được hiển thị cho người dùng khi họ thêm xe mới vào hệ thống.</p>
                    <p>Những tính năng phổ biến có thể là:</p>
                    <ul>
                        <li>Điều hòa</li>
                        <li>Hệ thống định vị GPS</li>
                        <li>Cảm biến lùi</li>
                        <li>Ghế da</li>
                        <li>Camera lùi</li>
                        <li>Bluetooth</li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css">
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
