@page
@model GotoCarRental.Areas.Admin.Pages.Features.EditModel
@{
    ViewData["Title"] = "Chỉnh sửa tính năng";
}

<div class="container py-4">
    <div class="row">
        <div class="col-md-8">
            <h1 class="display-6 fw-bold">Chỉnh sửa tính năng</h1>
            <nav aria-label="breadcrumb">
                <ol class="breadcrumb">
                    <li class="breadcrumb-item"><a asp-area="Admin" asp-page="/Index">Trang chủ</a></li>
                    <li class="breadcrumb-item"><a asp-page="./Index">Tính năng xe</a></li>
                    <li class="breadcrumb-item active" aria-current="page">Chỉnh sửa</li>
                </ol>
            </nav>
        </div>
    </div>

    <div class="row mt-3">
        <div class="col-md-8">
            <div class="card shadow-sm">
                <div class="card-body">
                    <form method="post">
                        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                        <input type="hidden" asp-for="Feature.Id" />
                        
                        <div class="mb-3">
                            <label asp-for="Feature.Name" class="form-label">Tên tính năng</label>
                            <input asp-for="Feature.Name" class="form-control" />
                            <span asp-validation-for="Feature.Name" class="text-danger"></span>
                        </div>
                        
                        <div class="mb-3">
                            <label asp-for="Feature.Description" class="form-label">Mô tả</label>
                            <textarea asp-for="Feature.Description" class="form-control" rows="3"></textarea>
                            <span asp-validation-for="Feature.Description" class="text-danger"></span>
                            <div class="form-text">Mô tả chi tiết về tính năng này (không bắt buộc).</div>
                        </div>
                        
                        <div class="d-flex justify-content-end mt-4">
                            <a asp-page="./Index" class="btn btn-outline-secondary me-2">
                                <i class="bi bi-arrow-left me-1"></i> Quay lại
                            </a>
                            <button type="submit" class="btn btn-primary">
                                <i class="bi bi-save me-1"></i> Lưu thay đổi
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
        
        <div class="col-md-4">
            <div class="card shadow-sm">
                <div class="card-header bg-light">
                    <h5 class="mb-0">Thông tin</h5>
                </div>
                <div class="card-body">
                    <p>Việc thay đổi tên tính năng sẽ ảnh hưởng đến hiển thị trên tất cả xe có tính năng này.</p>
                    <div class="alert alert-info mt-3 mb-0">
                        <i class="bi bi-info-circle-fill me-2"></i> Thay đổi sẽ được cập nhật ngay lập tức.
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css">
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
